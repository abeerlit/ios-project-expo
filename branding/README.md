# Tenant branding (icon + splash)

PNG assets copied here from `ios-project-expo/branding/<tenant>/` by `voxo sync eas`.
Must be **real files** in git — EAS Build only uploads this app repo (symlinks to ios-project-expo break on the build server).

/**
 * Same as bare ios-project/index.js — prime CallKeep before React mounts (Recents / killed state).
 */
import { runIosCallKitEntryBootstrap } from "../src/core/softphone/iosRecentsEntryBootstrap";

export { runIosCallKitEntryBootstrap };

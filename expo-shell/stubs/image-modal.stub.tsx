import React from "react";
import { View } from "react-native";

type Props = { children?: React.ReactNode };

export default function ImageModalStub({ children }: Props) {
  return <View>{children}</View>;
}
